定位：修改文件前必须先 read_file（盲改是错误的主要来源）。

- write_file 用于新建文件或整体重写；已存在文件的局部修改一律用 edit_file。
- edit_file 的 context 行选稳定锚点（函数签名的整行、独特注释），不要用空行或单个 `}`。
- 改动影响多个文件时逐文件小 patch，不要一次巨型 patch。
- 已有内容若被判定为冲突（文件在读取后又被修改），重新读取后再改，不要强推。