定位：shell 用于运行构建、测试、git 与探查命令。它是权限最重的工具。

- 优先用仓库声明的命令（见 AGENTS.md / package.json），不要臆造。
- 组合命令用 `&&` 串联，避免长驻前台进程；需要后台运行时明确说明。
- 命令失败时完整阅读 stdout+stderr，再决定下一步；不要机械重试同一命令。
- 不主动执行 git commit/push/分支切换等写历史操作，除非用户明确要求。
- 沙箱策略可能拒绝写工作区外的路径；被拒时修正命令，不要换写法绕过。