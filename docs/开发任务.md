---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '5006dba3-3ea1-4bfe-8022-69f2b92dc5ea'
  PropagateID: '5006dba3-3ea1-4bfe-8022-69f2b92dc5ea'
  ReservedCode1: '8fb914a6-c3b3-45e8-bac6-a2e2257503f5'
  ReservedCode2: '8fb914a6-c3b3-45e8-bac6-a2e2257503f5'
---

# 墨子 Mozi · 开发任务文档

> 配套设计：`仿Codex-Agent开发技术方案.md`（总体架构 / 路线图）、`Mozi-详细设计文档.md`（模块级深化）
> 本文是**开发执行清单**：每完成一个任务，在 `[ ]` 前打 `[x]` 并填完成日期。
> 命名：中文叙述用「墨子」，命令/包名/代码统一小写 `mozi`，引擎包 `@mozi/core`。

---

## 一、里程碑总览

| 里程碑 | 目标 | 状态 | 备注 |
|--------|------|------|------|
| M0 工程基建 | 脚手架、共享 DTO 契约、CI 分层规则 | ✅ 已完成 | pnpm workspace / turbo / biome / tsconfig.base / 13 包骨架已就绪 |
| **M1 引擎闭环** | 终端跑通一次真实编码任务 | ✅ 已完成 | 主循环/事件/会话持久化/ScriptedProvider 测试就绪 |
| M2 安全与编辑 | PatchEngine / 风险分析 / 压缩 / 子智能体 v1 | ✅ 已完成 | 2026-09-11 Auto-Compact / todo_list / 新鲜度 / 子智能体 v1 |
| M3 沙箱与生态 | L2/L3 沙箱 / MCP stdio / planner-executor | ✅ 已完成 | 2026-09-11 四档沙箱 + 会话 fork/GC |
| M3.5 完整 MCP 客户端 | Streamable HTTP / OAuth / Sampling / Elicitation | ✅ 已完成 | 2026-09-11 全能力 MCP 客户端 |
| M4 桌面应用 | Electron / IpcBridge / Monaco Diff | ✅ 已完成 | 2026-09-11 会话池 + IPC 契约 + Diff 审阅 + 设置密钥 + MCP 管理 + 渲染进程 |
| M4.5 定时任务 | tick 调度 / 无人值守 / worktree 隔离 | ✅ 已完成 | 2026-09-11 调度求解 + 无人值守策略 + worktree 产物 + 三层锁 + 通知 + CLI 命令族 |
| M4.75 移动端 | @mozi/protocol / WSS / 中继 / Expo | ✅ 已完成 | 2026-09-11 远程协议 + E2E 加密 + 中继 + CLI 设备命令族 + 移动端骨架；真实 WSS/Expo 接入待 pnpm 依赖 |
| M4.9 学习扩展 | 提示词工程 / 记忆 / 多模态 / Hooks | ⬜ 跳过 | 用户决策不推进（M4.9 WIP 留在工作区未提交） |
| M5 开源运营 | 评测集 / 文档站 / 1.0 发布 | ✅ 代码侧完成 | 2026-09-11 评测集 30 任务 + 文档站 + 治理 + CI；1.0.0 发布与 30 天跑分趋势为运营项待启动 |

---

## 二、当前迭代：M1 引擎闭环（详细任务）

设计依据：技术方案 §7 M1 + 详细设计 D0/M1/M2/M3/M4/M7 + 首周启动清单（Day1-7）。
实现原则：**零重依赖**（fetch 实现 OpenAI 兼容 Provider，不引 openai SDK；自实现轻量 glob；原生校验替代 ajv），保证 `pnpm i` 干净、测试用 ScriptedProvider 零 API 成本。

### T1 共享数据契约 `@mozi/shared`
- [x] 2026-09-11 `src/events.ts`：AgentEvent 全集（会话/轮次/工具/上下文/用量/终止/debug）、`TaskCompleteReason`、`ApprovalReason`
- [x] 2026-09-11 `src/messages.ts`：ChatMessage / UserContent / AssistantMessage / ToolCall / ToolResult / DisplayPayload / TokenUsage / AgentError
- [x] 2026-09-11 `src/policy-types.ts`：PolicyMode / PolicyConfig / PolicyRule / PolicyDecision
- [x] 2026-09-11 `src/errors.ts`：ErrorCodes 常量
- [x] 2026-09-11 `src/config.ts`：SessionConfig / SessionLimits / RunInput / SessionSnapshot
- [x] 2026-09-11 `src/index.ts`：统一再导出

### T2 LLM Provider 层 `@mozi/providers`
- [x] 2026-09-11 `src/types.ts`：ChatRequest / LLMProvider / LLMStream / StreamEvent / ProviderCapabilities
- [x] 2026-09-11 `src/aggregator.ts`：StreamAggregator（toolcall 参数按 index 拼装 + 容错）
- [x] 2026-09-11 `src/scripted.ts`：ScriptedProvider（确定性测试基建，录制响应序列回放）
- [x] 2026-09-11 `src/openai.ts`：OpenAICompatibleProvider（fetch + SSE 解析，覆盖 DeepSeek/Qwen/GLM/Ollama）
- [x] 2026-09-11 `src/registry.ts` + `src/index.ts`：ProviderRegistry + 能力协商

### T3 工具系统 `@mozi/tools`
- [x] 2026-09-11 `src/types.ts`：AgentTool / ToolContext
- [x] 2026-09-11 `src/read.ts`：read_file（行区间/行号前缀/二进制探测）
- [x] 2026-09-11 `src/write.ts`：write_file（幂等建父目录）
- [x] 2026-09-11 `src/glob.ts`：glob（自实现轻量 glob，支持 `**`/`*`/`?` + gitignore 合并）
- [x] 2026-09-11 `src/grep.ts`：grep（递归正则，文件:行号:内容，上限截断）
- [x] 2026-09-11 `src/shell.ts`：shell（child_process spawn，L1 超时强杀，输出截断）
- [x] 2026-09-11 `src/registry.ts` + `src/index.ts`：ToolRegistry（注册/校验/groupBySafety/ schemas）

### T4 引擎核心 `@mozi/core`
- [x] 2026-09-11 `src/session/session-store.ts`：SessionStore（JSONL 追加写 + resume 重放 + meta.json）
- [x] 2026-09-11 `src/context/context-manager.ts`：ContextManager（基础版：系统提示+工具定义+历史裁剪+AGENTS.md 注入）
- [x] 2026-09-11 `src/engine/agent-engine.ts`：AgentEngine（状态机主循环 / 中断 / 失败重试 / 读并行写串行）
- [x] 2026-09-11 `src/index.ts`：导出 Engine + 工厂构造

### T5 策略引擎 `@mozi/policy`
- [x] 2026-09-11 `src/index.ts`：PolicyEngine（三档模式 readonly/auto/full-auto + 内置安全规则 + 评估算法）

### T6 CLI 入口 `apps/cli`
- [x] 2026-09-11 `src/index.ts`：commander 入口 + REPL（readline）+ `mozi exec --json` 非交互模式
- [x] 2026-09-11 补装 `commander` 依赖

### T7 测试与验收
- [x] 2026-09-11 补装 `vitest` 依赖
- [x] 2026-09-11 `packages/core/src/__tests__/engine.test.ts`：ScriptedProvider 驱动完整 loop（审批流/中断/步数上限/失败重试/JSONL 落盘可回放）
- [x] 2026-09-11 `packages/providers/src/__tests__/message-converter.test.ts`：四种 Provider 格式转换全路径测试（14 用例）
- [x] 2026-09-11 `pnpm build` 全绿、`pnpm test` 全绿、`pnpm lint` 全绿

### 验收标准（M1 DoD）
- [x] 2026-09-11 用 ScriptedProvider 集成测试覆盖 loop 全路径（已完成）
- [ ] 真实 LLM 端到端：`mozi "读取本目录 README 并统计字数"` 全链路跑通（需用户提供 API Key 后验证）
- [ ] resume 会话可继续对话（逻辑已实现，待真实环境验证）

---

## 四、当前迭代：M2 安全与编辑（进行中）

设计依据：技术方案 §7 M2 + 详细设计 D0/M2/M3/M5/M6/M7。

### T1 PatchEngine + /undo
- [x] 2026-09-11 `packages/tools/src/patch-parser.ts`：apply_patch 语法解析（Update/Add/Delete + hunk signature/additions，`@@` 锚点行跳过）
- [x] 2026-09-11 `packages/tools/src/patch-matcher.ts`：hunk 线性扫描匹配（尾随空白容忍 + 顺序推进 + 前缀回退 + 最近匹配诊断）
- [x] 2026-09-11 `packages/tools/src/patch-applier.ts`：事务型应用器（Phase1 全量只读匹配 → Phase2 快照+写入 → 失败回滚，任一失败整体拒绝）
- [x] 2026-09-11 `packages/tools/src/edit-file.ts`：edit_file 工具（riskLevel=exec，diff display，错误归一化 errorKind）
- [x] 2026-09-11 `packages/tools/src/workspace.ts`：补 readFile/writeFile（tmp+rename 原子写）/deleteFile
- [x] 2026-09-11 `packages/tools/src/snapshots.ts`：saveSnapshot/listSnapshots/undoLast（ABSENT 标记支持「撤销新建文件」）
- [x] 2026-09-11 edit_file 持久化会话级快照（`.mozi/snapshots/<sessionId>`，已 gitignore）
- [x] 2026-09-11 CLI REPL `/undo` 命令：弹出最近一组快照恢复（含删除新建文件）
- [x] 2026-09-11 `packages/tools/src/__tests__/patch.test.ts`：22 用例（parser/matcher/applier 原子性/edit_file/200 轮 fuzz）

### T2 命令风险分析 + PolicyRule 自定义 + 审批卡片升级
- [x] 2026-09-11 `packages/policy/src/risk-analyzer.ts`：命令分段（管道/`&&`/`;` 切分）+ 综合评级（safe/side-effect/network/high）+ 子命令表（git/npm）+ `curl|sh`/设备文件重定向 → high
- [x] 2026-09-11 `packages/policy/src/index.ts`：`PolicyEngine.evaluate` 规则优先序（自定义 rules → 内置 → shell 风险分析 → 模式回退表）；`unattended` 时 ask→deny（M13 I1）
- [x] 2026-09-11 `packages/shared/src/policy-types.ts`：`PolicyRule`（tool/commandPattern/pathGlob → allow/ask/deny）+ `ApprovalReason.kind='risk'` 结构化 segments
- [x] 2026-09-11 CLI 审批卡片升级：按 `reason.kind` 渲染风险分段（red/yellow 标记 + matchedRule）、policy 规则、manual note（父会话与子智能体共用 `presentApproval`）

### T3 Auto-Compact + todo_list + 文件新鲜度
- [x] 2026-09-11 `packages/core/src/context/compactor.ts`：`estimateTokens`/`shouldCompact`/`selectCompactRegion`/`summarize`/`compactMessages`（首次压缩不受最小间隔约束；失败跳过不阻塞）
- [x] 2026-09-11 `packages/core/src/engine/agent-engine.ts`：主循环每步前 `maybeCompact` → 发 `context.compacted` + 重置计数；`session.meta.turnsSinceLastCompact/compactCount`
- [x] 2026-09-11 `packages/core/src/context/context-manager.ts`：`BuildView`（含 estimatedTokens）+ 脏文件 system 提示注入 + `budgetFor(session)`
- [x] 2026-09-11 `packages/tools/src/todo-list.ts`：`todo_list`（list/update，会话级 `session.meta.todos`，`[ ]/[~]/[x]` 渲染，riskLevel=meta）
- [x] 2026-09-11 `packages/core/src/session/session-store.ts`：`saveMeta`/resume 恢复 todos；`SessionMeta` 开放索引签名
- [x] 2026-09-11 `packages/core/src/context/freshness.ts`：`FreshnessTracker`（markRead/checkDirty/relativize）；引擎 read_file/edit_file 后记基准，shell 后重扫

### T4 子智能体编排 v1（task 工具 + 模板 + 审批冒泡）
- [x] 2026-09-11 `packages/core/src/subagent/templates.ts`：`AgentTemplate` + 内置 explore/general/reviewer + `.mozi/agents/*.md`、`.claude/agents/*.md`、`~/.mozi/agents/*.md` frontmatter 加载
- [x] 2026-09-11 `packages/core/src/subagent/supervisor.ts`：`SubAgentSupervisor`（并发槽 FIFO + queued 事件、maxDepth/maxPerTurn、级联取消、超时、摘要截断回传）
- [x] 2026-09-11 权限单调收紧：`tighten(parent, template)` 取更严 + `intersectTools` 求交；子会话 config 独立
- [x] 2026-09-11 审批冒泡：子 `tool.approval.required` → 宿主通道 `subagent.approval.required`；`resolveApproval(parent, subCallId)` 路由；`InteractiveApprovalGateway` 早期裁决缓存消竞态
- [x] 2026-09-11 事件桥接：`subagent.started/queued/progress/approval.required/completed/failed`（§12.8）；宿主 `onEvent` 出口 + `emitLive`
- [x] 2026-09-11 持久化：子会话落盘 `subs/<id>/events.jsonl` + `meta.json`（单一事实源，无重复写入）
- [x] 2026-09-11 `packages/tools/src/task.ts` + registry（meta 级并入并行组）+ `createSub`（SessionStore）
- [x] 2026-09-11 `packages/providers/src/scripted.ts`：多会话脚本 `scenarios`（sessionId 通配分派，§12.13）
- [x] 2026-09-11 `packages/core/src/__tests__/m2.test.ts`：18 用例（todo/新鲜度/压缩/模板/收紧/截断/spawn/深度/每轮上限/只读拒绝/审批冒泡）
- [x] 2026-09-11 运行验证：M2-A/B/C/D 共 69 断言全通过；全仓 112 单测通过；13 包构建 EXIT 0

---

## 四之二、M3 沙箱与生态（已完成）

设计依据：详细设计 §6.4 沙箱 + §M7 会话持久化 + 技术方案 §7 M3。

### T1 沙箱契约与四档实现
- [x] 2026-09-11 `packages/shared/src/sandbox.ts`：SandboxLevel / SandboxOptions / SandboxResult / SandboxRunner 跨层契约
- [x] 2026-09-11 `packages/sandbox/src/platform.ts`：平台探测（macOS Seatbelt / Linux Landlock 尽力 / Windows Job Object 尽力）+ docker 检测
- [x] 2026-09-11 `packages/sandbox/src/network.ts`：网络白名单（默认 registry.npmjs.org / pypi.org / github.com 等）+ mergeAllowNet 去重
- [x] 2026-09-11 `packages/sandbox/src/exec.ts`：L0 直执行 / L1 进程组超时强杀 / L2 Seatbelt / L3 Docker `--network none` + `-v` 挂载；AbortSignal 联动
- [x] 2026-09-11 `packages/sandbox/src/select.ts`：选择链 + 优雅降级（L2 无 Seatbelt→L1、L3 无 docker→L2/L1，记录 degradedFrom/note）
- [x] 2026-09-11 `packages/sandbox/src/__tests__/sandbox.test.ts`：选择链/网络/降级/执行单测（16 断言运行验证全通过）

### T2 沙箱接线
- [x] 2026-09-11 `packages/tools/src/types.ts`：ToolContext 增可选 `sandbox?: SandboxRunner`
- [x] 2026-09-11 `packages/tools/src/shell.ts`：有 sandbox 时经沙箱执行并回填 `meta.sandboxLevel/sandboxDegradedFrom/sandboxNote`；无则保持直 spawn
- [x] 2026-09-11 `packages/shared/src/messages.ts`：ToolResult.meta 增 sandbox* 字段
- [x] 2026-09-11 `packages/core/src/engine/agent-engine.ts`：EngineDeps.sandbox 透传至 ToolContext
- [x] 2026-09-11 `packages/core/src/engine/factory.ts`：`sandboxLevel` / `allowNet` 选项按需构建沙箱

### T3 会话 fork / list / GC（M7）
- [x] 2026-09-11 `packages/core/src/session/session-store.ts`：`fork(srcId, atEventIndex?, newId?)` 复制前 N 条事件 + meta（记录 forkedFrom）；`gc(retentionDays=30)` 移入 .trash；`restore(id)` / `listTrashed()`；list 排除点目录
- [x] 2026-09-11 运行验证：fork 全量/截断/独立性、gc 保留/回收、restore 共 11 断言全通过

---

## 四之三、M3.5 完整 MCP 客户端（已完成）

设计依据：详细设计 M8 §8.1–§8.15。

### T1 传输层
- [x] 2026-09-11 `packages/mcp-client/src/transport/types.ts`：McpTransport 抽象 + JSON-RPC 消息类型 + 三 kind
- [x] 2026-09-11 `packages/mcp-client/src/transport/stdio.ts`：子进程 spawn（换行分隔 JSON）、stderr 转日志、SIGTERM→1s→SIGKILL、Windows .cmd shell 处理
- [x] 2026-09-11 `packages/mcp-client/src/transport/streamable-http.ts`：单端点 POST + Mcp-Session-Id + SSE 解析 + GET 推送流 + DELETE 终止；HttpSseLegacyTransport 旧版降级
- [x] 2026-09-11 `packages/mcp-client/src/transport/mock.ts`：InMemoryTransport（测试桩）

### T2 JSON-RPC 与连接
- [x] 2026-09-11 `packages/mcp-client/src/jsonrpc.ts`：请求/响应关联、超时/AbortSignal、通知分发、server→client 反向请求路由
- [x] 2026-09-11 `packages/mcp-client/src/connection.ts`：initialize 握手 + 能力协商、30s ping 心跳 + degraded 标记、状态机（disconnected→connecting→connected→degraded→offline）、反向请求（sampling/elicitation/roots/ping）、通知（tools/resources/progress/logging/cancelled）

### T3 能力桥接
- [x] 2026-09-11 `packages/mcp-client/src/tools.ts`：McpToolAdapter（`<serverId>__` 前缀、描述截断、content 渲染、image/audio 落盘、AbortSignal 联动）
- [x] 2026-09-11 `packages/mcp-client/src/bridge.ts`：McpBridge（多 server 聚合、tools 热更新、Prompts→/mcp: 命令、内置 mcp_read_resource 工具、$VAR 环境变量解析、OAuth 注入）
- [x] 2026-09-11 `packages/mcp-client/src/oauth.ts`：PKCE 生成、RFC 9728 元数据发现、授权码/刷新流程、TokenStore 抽象

### T4 事件与核心集成
- [x] 2026-09-11 `packages/shared/src/events.ts`：`mcp.server.status` / `mcp.sampling.requested|resolved` / `mcp.elicit.requested|resolved` 事件
- [x] 2026-09-11 `packages/core/src/engine/factory.ts`：`mcpServers` / `onMcpEvent` 选项 + `createEngineAsync()` + `McpBridge` 暴露 + `dispose()`
- [x] 2026-09-11 运行验证：握手/工具发现/工具调用/sampling/状态事件/registry 集成共 6+6 断言全通过
- [x] 2026-09-11 `packages/mcp-client/src/__tests__/mcp.test.ts`：3 用例（握手发现 / 适配器渲染 / sampling 反向请求）

---

## 四之四、M4 桌面应用（已完成）

设计依据：详细设计 §M10（§10.2 进程架构 / §10.3 IPC 协议 / §10.4 会话并发 / §10.5 关键页面 / §10.6 密钥存储 / §10.7 自动更新 / §10.8 性能 / §10.9 测试）。
实现约束：本机无 `pnpm install`（electron / react / vite 未安装），故把**传输无关的核心**全部落地为可在纯 Node 中测试的代码，Electron 外壳以「晚绑定最小接口 + ambient 类型垫片」保持 `tsc -b` 全绿。

### T1 IPC 协议契约 `@mozi/protocol`（§10.3）
- [x] 2026-09-11 `packages/protocol/src/index.ts`：19 条 invoke 通道 + 4 条 send 通道的泛型映射（`InvokeChannels` / `SendChannelMap`）、`INVOKE_CHANNELS` / `SEND_CHANNELS` 清单、`ChannelServer` / `ChannelClient` 抽象、`MaybePromise<T>`
- [x] 2026-09-11 `approvalTicketFromEvent`：父 / 子审批事件统一提取 `ApprovalTicketView`
- [x] 2026-09-11 `packages/protocol/src/loopback.ts`：`LoopbackChannel`（进程内 loopback transport，供测试 / TUI 复用同一 handler 注册表）
- [x] 2026-09-11 语义统一：移动端 WSS 协议 = 桌面 IPC 协议 = loopback，零分叉（§14 原则 3）

### T2 会话池与事件扇出 `AgentService`（§10.2 / §10.4）
- [x] 2026-09-11 `packages/desktop/src/main/agent-service.ts`：每会话独立 `CreatedEngine`（`pool`）→ 天然多会话并行；`create/resume/fork/delete/list`
- [x] 2026-09-11 `start()` 立即返回 `RunStartResponse`，后台 `drive()` 消费生成器并维护状态机（idle→running→pending_approval→completed/failed）
- [x] 2026-09-11 事件分桶：`stamp()` 为无 sessionId 的引擎事件补上归属会话（多会话 UI 分桶正确）
- [x] 2026-09-11 窗口管理：`attachWindow` / `detachWindow`（alreadyOpen + focus），窗口关闭 ≠ 会话销毁（`background` 默认开）
- [x] 2026-09-11 `shutdown(timeoutMs)`：全部会话 abort + dispose，超时兜底
- [x] 2026-09-11 `dashboard()` / `audit()`：从会话事件流聚合用量 / 工具调用 / 审批记录

### T3 IpcBridge 与 Electron 传输（§10.2 / §10.3）
- [x] 2026-09-11 `packages/desktop/src/main/ipc-bridge.ts`：把 §10.3 全通道落到 `AgentService`；事件扇出 `onEngineEvent` / `onSessionStatus`
- [x] 2026-09-11 `packages/desktop/src/main/electron-transport.ts`：`ElectronChannelServer`（ipcMain.handle / webContents.send）+ `IPC_PREFIX='mozi:'`
- [x] 2026-09-11 `packages/desktop/src/preload.ts`：`buildMoziApi` + 白名单 `ALLOWED_INVOKE` / `ALLOWED_SEND`（contextIsolation + sandbox，渲染进程零 Node）
- [x] 2026-09-11 `packages/desktop/src/main/electron-main.ts`：`boot()` 晚绑定 electron，创建窗口 + 退出钩子

### T4 Diff 审阅 + 逐 hunk 批准（§10.5③）
- [x] 2026-09-11 `packages/desktop/src/main/diff-service.ts`：LCS 最小编辑脚本 → `buildHunks`（连续变更聚合、稳定 `h1..hN` id、行范围）；`applyHunksToContent` 仅应用选中 hunk
- [x] 2026-09-11 `DiffReviewService`：`record` / `get` / `applyPartial` / `applyAll` / `clear`（部分应用后重算剩余 hunk）
- [x] 2026-09-11 `packages/desktop/src/shared/diff-view.ts`：`languageFor`（扩展名→Monaco 语言）+ `hunkDecorations`（original/modified 两组 decoration），主/渲染进程共用
- [x] 2026-09-11 `renderer/components/DiffReviewer.tsx`：Monaco DiffEditor 封装（运行时注入 `MonacoLike`）+ hunk 勾选面板

### T5 设置与密钥安全（§10.6）
- [x] 2026-09-11 `packages/desktop/src/main/settings-store.ts`：`SettingsStore`（合并式 patch + 原子落盘 tmp+rename）
- [x] 2026-09-11 `SafeStorageLike` 抽象 + `setApiKey`（OS 级加密；不可用时拒绝明文并提示环境变量）+ `getApiKey`（**密文在 safeStorage 不可用时绝不回退为明文密文**）+ 环境变量兜底
- [x] 2026-09-11 `maskSecret` + `listProviders`（只回 `hasApiKey` + 脱敏预览，密钥绝不进渲染进程）
- [x] 2026-09-11 `testProvider` / `costLimits` / `appendAllowRule`（onceForSession）

### T6 MCP 管理中心（§10.5⑦）
- [x] 2026-09-11 `packages/desktop/src/main/mcp-manager.ts`：`list/add/remove/restart` + `setSampling`（三档）/`setTrusted`/`setAllowedTools`；连接失败 → offline

### T7 渲染进程与测试（§10.5 / §10.9）
- [x] 2026-09-11 `packages/desktop/src/shared/render-item.ts`：`RenderItem` 联合 + `eventToRenderItems`（与 M9/M10/M14 共享渲染语义）
- [x] 2026-09-11 `packages/desktop/src/shared/store.ts`：`UiStore`（纯 reducer `applyEngineEvent` + `useSyncExternalStore` 订阅），delta 流式合并 / 工具卡更新 / 审批去重 / 子智能体树 / 用量
- [x] 2026-09-11 `renderer/`：`App.tsx`（标签页）/ `Sidebar.tsx`（会话侧栏 + 状态徽标）/ `SessionView.tsx`（会话视图 + 计划面板 + 上下文面板）/ `SettingsPanel.tsx` / `Dashboard.tsx`（SVG 图表）/ `SubAgentPanel.tsx`（进度环）/ `ApprovalCard.tsx`
- [x] 2026-09-11 `renderer/types/react-shim.d.ts`：React/JSX ambient 类型垫片（无 react 依赖下渲染进程可 `tsc --noEmit`）
- [x] 2026-09-11 `index.html` / `vite.config.ts` / `vitest.config.ts`：CSP 收紧、渲染构建、独立测试配置
- [x] 2026-09-11 `packages/desktop/src/__tests__/m4.test.ts`：47 用例（IPC 契约 / 会话池并行 / 审批路由 / Diff hunk / 设置密钥 / MCP / UI store / preload 白名单 / E2E）
- [x] 2026-09-11 运行验证：M4 47 用例全通过；全仓 159 单测通过；9 包 + 渲染进程构建 EXIT 0

### 关键修复（M4 期间）
- **审批死锁**：`AgentService` 后台消费生成器，而 `executeOne` 在等待审批时（审批事件只在生成器 yield，未上抛）→ 宿主与引擎互相等待。修法：引擎 `push()` 对 `tool.approval.required|resolved` 额外走 `hostSink`，宿主可在阻塞前登记 pending 并 resolve（`packages/core/src/engine/agent-engine.ts`）。
- **密钥回退缺陷**：safeStorage 不可用时 `getApiKey` 曾把密文当明文返回，会导致上游把 ciphertext 当 API Key 发出。修为「不可解密即视为无密钥，回退环境变量」。
- **事件双份**：审批事件经宿主通道上抛后，`AgentService.resolveApproval` 又手动 emit 一次；已移除手动 emit，并在 pool / store 两侧按 callId 去重。

---

## 四之五、M4.5 定时任务与无人值守（已完成）

设计依据：技术方案 §7 M4.5 + 详细设计 M13（§13.1–§13.12）。
实现约束：无 pnpm 环境下核心全部为纯 Node 可测代码；cron 求解零依赖（Intl 时区，替代 cron-parser）。

### T1 调度核心 `packages/core/src/tasks/`
- [x] 2026-09-11 `cron.ts`：5 字段 cron 解析（`*/n`/`a-b`/列表/周 0·7 归一）+ `nextRunAfter`（Intl 时区，Vixie 日/周 OR 语义，DST 间隙自动跳过）
- [x] 2026-09-11 `solver.ts`：`nextRunAt`（cron/interval/once 统一求解）+ `countMissedRuns` + `planTick`（skip / catch-up-once / catch-up-all 补跑；once 过期立即补跑）
- [x] 2026-09-11 `store.ts`：`TaskStore`（tasks.json 原子写 tmp+rename + .bak 恢复 + schemaVersion）
- [x] 2026-09-11 `locks.ts`：三层文件锁（tick / task / workspace），原子 mkdir + holder.json（pid/时间戳），僵尸锁（进程不存在或超龄）自动抢占
- [x] 2026-09-11 `notify.ts`：通用 webhook POST（10s 超时 + 重试 1 次 + maskSecret 深度脱敏）+ 载荷 schema（event/taskId/status/usage/summary/branch/prUrl）

### T2 无人值守安全模型 `unattended.ts`
- [x] 2026-09-11 三档策略 → PolicyMode + 规则合成：readonly（全只读）/ allowlist（白名单 allow + 白名单外 deny + 内置高危 deny 不可绕过 I3）/ full（全放行）
- [x] 2026-09-11 静态校验（task add 时）：full+direct 拒绝、full+L2 拒绝（I4）、allowlist 空名单拒绝、readonly+branch 警告
- [x] 2026-09-11 引擎接入：`AgentEngine.evaluateOptions`（unattended 注入，I1 ask→deny）+ `createEngine({ unattended })`；默认无人值守 system prompt

### T3 headless 执行与产物隔离 `headless.ts` + `runner.ts` + `git.ts`
- [x] 2026-09-11 `runHeadless`：组装无人值守引擎（approval=deny 兜底 + 无人值守评估 + maxSteps 收紧 + 超时 AbortController）+ 事件收集 + 报告生成
- [x] 2026-09-11 `executeTaskOnce`：前置检查 → worktree 隔离（`git worktree add -b mozi/task/<id>/<runId>`，非 git 降级目录快照 + changes.patch）→ 产物收集（branch commit / direct / report-only）
- [x] 2026-09-11 运行记录 `runs/<taskId>/run-<ts>.json`（status/usage/artifacts/trigger/missedRuns）+ `report.md`

### T4 tick 主循环 `scheduler.ts`
- [x] 2026-09-11 `TaskScheduler.tick()`：L1 tick 锁互斥 → 逐任务 `planTick`（先推进 nextRunAt 防崩溃重复）→ L2 任务锁 overlap（skip）→ `runTask` → 状态更新 + 连续失败 ≥3 熔断停用 + 通知
- [x] 2026-09-11 `runNow()`：手动触发与定时同一执行链路
- [x] 2026-09-11 CLI 冒烟：`mozi task add/list/doctor/tick/gc` 全通（`--policy` 与顶层 exec `--policy-mode` 命名隔离，修复 commander 父子同名选项抢占）

### T5 测试与验证
- [x] 2026-09-11 `packages/core/src/__tests__/m4-45.test.ts`：vitest 用例（cron 边界/时区、补跑策略、I1/I3/I4、存储、锁、webhook、tick、worktree、headless）
- [x] 2026-09-11 运行验证：独立 node ESM 脚本 **21/21 全绿**（本环境 vitest 因 pnpm 虚拟依赖缺失不可跑，测试代码仍入库待 `pnpm i` 后执行）
- [x] 2026-09-11 修复预存问题：`screenshot.ts` 返回 meta 结构（image 应走 DisplayPayload）、core 入口移除未完成 memory 导出（estimateTokens 重名冲突）、shared dist 重建

### M4.5 轮决策记录
- **cron 零依赖自研**：`cron-parser` 在无 pnpm 环境不可装；自研 Intl 时区求解（试探-校正法），`tzToUtc` 偏移修正方向是关键坑（符号反了会震荡不收敛）。
- **tick 进程内执行**：库内 `tick()` 默认内联 `runTask`（可测、无额外进程），OS 注册的 `mozi task tick` 由 CLI 调用同一路径；锁保证 overlap。
- **once 语义**：过期即首次 tick 立即补跑一次并 disable（无论成败）；`planTick` 首次调度分支特判 once-due。
- **子命令 option 冲突**：commander 父子命令同名 `--policy` 时父级抢占参数；顶层 exec 改 `--policy-mode`，`--policy` 专属 task。

---

## 四之六、M5 开源运营与评测体系（代码侧完成）

设计依据：技术方案 §7 M5 + §8.3 评测集 + §9 开源工程建设。

### T1 评测集 benchmark/（30 任务 + runner）
- [x] 2026-09-11 任务集：L1×10（单点修改）/ L2×10（跨文件功能）/ L3×5（调试型·预埋 bug + node:test 锁定测试文件）/ L4×5（重构·行为不变式对拍）
- [x] 2026-09-11 任务结构：`task.md`（自然语言指令）+ `setup.mjs`（Node 脚本初始化，跨平台替代 bash）+ `solution/`（参考解）+ `asserts/check.mjs`（行为断言）
- [x] 2026-09-11 `benchmark/runner/run.mjs`：smoke 模式（setup→参考解→断言，零 API 成本验证评测集自洽）+ live 模式（真实模型跑分：成功率/步数/token/审批次数/耗时，@mozi/core 引擎 + auto-approve + full-auto + 超时强停）
- [x] 2026-09-11 报告产出：`results/<ts>-<mode>.json/.md`（按模型分列字段，官网趋势图数据源）
- [x] 2026-09-11 运行验证：smoke 30/30 全绿（L1 10/10、L2 10/10、L3 5/5、L4 5/5）

### T2 嵌入示例 examples/
- [x] 2026-09-11 `examples/embed-minimal/`：50 行引擎嵌入（工厂组装 + 事件流消费 + ScriptedProvider 零成本演示 + 真实模型切换注释）
- [x] 2026-09-11 `examples/custom-tool/`：工具扩展示例（AgentTool 契约 + 手工组装 AgentEngine + 自定义 word_count 工具）
- [x] 2026-09-11 运行验证：两个示例均实际跑通（含 provider alias 修正）

### T3 文档站 website/（VitePress）
- [x] 2026-09-11 快速开始 / 配置手册（环境变量·策略三档·风险分析·限制·沙箱）/ 工具扩展开发 / 引擎嵌入指南 / 安全模型白皮书 / 评测页
- [x] 2026-09-11 API 参考：Engine（@mozi/core）/ Tools（@mozi/tools）/ Providers（@mozi/providers）/ Events & DTO（@mozi/shared）
- [x] 2026-09-11 构建验证：vitepress build 成功；GitHub Pages 部署 workflow 就绪（docs.yml）

### T4 社区治理与工程规范
- [x] 2026-09-11 CODE_OF_CONDUCT.md（Contributor Covenant v2.1）/ SECURITY.md（漏洞报告渠道 + 安全关注面）/ CODEOWNERS / PR 模板 / Issue 模板（bug + feature）
- [x] 2026-09-11 CONTRIBUTING.md 充实：30 分钟上手指南（M5 DoD）+ 仓库地图 + PR 检查单 + changesets 流程
- [x] 2026-09-11 `.changeset/config.json` 修正：`access: public`、repo 指向 programmerguohuajing/mozi-agent、`baseBranch: master`（原为 main 与实际分支不符）、忽略不发布包（mobile/desktop/benchmark/examples/website）

### T5 CI/CD
- [x] 2026-09-11 `benchmark.yml`：每日 cron smoke（评测集自洽零成本）+ 手动 live（MOZI_BENCH_* secrets，结果 commit 回 results/ 供趋势图）
- [x] 2026-09-11 `docs.yml`：VitePress → GitHub Pages（website/** 变更触发）
- [x] 2026-09-11 `release.yml`：changesets 版本 PR + npm 发布（发布由 changeset 声明驱动，不产生未经维护者决定的版本变更）

### 未含项（明确说明）
- [ ] **1.0.0 发布**：版本号由用户决定（长期约束），changesets 流水线已就绪，待用户启动
- [ ] **eval 每日跑分无中断 30 天**（M5 DoD）：CI 已就位，属时间积累项
- [ ] **官网公开趋势图**：文档站评测页已预留数据位，待 live 跑分数据积累后由 benchmark.md 展示
- [ ] **定时任务运营化（任务模板库/状态页）**：依赖 M4.5 定时任务未实现，前置不满足

### M5 轮决策记录
- **setup.mjs 而非 setup.sh**：CI 三平台 + Windows 本地开发都能跑，bash 依赖会把评测集锁死在 Linux。
- **smoke 模式是评测集的自体检**：参考解必须过全部断言，防止「任务本身不可解 / 断言写错」腐烂评测集；CI 每日零成本跑。
- **L3 测试文件锁定**：调试型任务的 test/ 不许改是任务语义的一部分，防止模型「改测试交差」。
- **L4 行为不变式对拍**：重构类任务在断言里内嵌原实现的对拍逻辑，验证「行为不变」而非「输出像样」。
- **website 纳入 pnpm workspace**：与其他包统一依赖管理（vitepress 只装一处），.changeset ignore 防止误发布。

---

## 四之七、M4.75 移动端与远程访问（已完成）

设计依据：详细设计 M14（§14.1–§14.13）+ 技术方案 §7 M4.75。
实现约束：无 pnpm 环境下全部为纯 Node 可测代码；E2E 用 Node crypto 零依赖（替代 libsodium）。

### T1 远程协议 `packages/protocol/src/remote.ts`
- [x] 2026-09-11 设备权限矩阵：`approvalPermitted`（standard：safe/network 可批、high 仅 defer；all 全批；none 全拒）+ `DevicePermissions` 契约
- [x] 2026-09-11 配对：`PairingService`（6 位码 / 5min TTL / 单次使用 / 10 次失败暴力锁定）+ `hashToken`（sha256，注册表不存明文）
- [x] 2026-09-11 设备注册表：`DeviceRegistry`（tokenHash 校验 / 吊销 / rename / lastSeenAt）+ `EventLog`（seq 游标 + replaySince）+ `RequestDeduper`（幂等去重）+ `externalizeAttachment`（>8KB 附件外置 contentId）

### T2 E2E 加密 `packages/protocol/src/crypto-box.ts`
- [x] 2026-09-11 ECDH P-256（prime256v1）派生 + AES-256-GCM 密封；信封零内容（中继只见 {to,len,ts}）
- [x] 2026-09-11 Ed25519 内层签名（signMessage/verifySignature 防中继伪造）+ 篡改检测

### T3 节点服务 `packages/protocol/src/remote-node.ts`
- [x] 2026-09-11 `RemoteNode`：帧通道（pair→auth→invoke→attach/fetch）、权限门禁（CHANNEL_PERMISSION 映射）、high 审批 defer、requestId 幂等（result await 后回传）、事件扇出 + lastEventId 断线重放、附件外置、revokeCurrent 即时吊销

### T4 中继服务器 `packages/relay-server/`
- [x] 2026-09-11 `RelayRouter`：零内容信封路由（在线转发 / 离线未交付）+ 日志开关只记路由头；`NtfyPushGateway`；包导出结构

### T5 CLI 集成 `apps/cli/src/remote.ts`
- [x] 2026-09-11 `mozi device pair/list/revoke/rename` + `mozi serve`（配对码横幅 + 文件式注册表 MOZI_DEVICES_FILE 原子写）；CLI 冒烟 10/10

### T6 移动端骨架 `apps/mobile/`
- [x] 2026-09-11 `src/client.ts`（视图模型 + RemoteClient 抽象）+ tsconfig/package.json/README（type:module）；真实 Expo 构建待 pnpm 依赖

### T7 测试与验证
- [x] 2026-09-11 `packages/protocol/src/__tests__/m4-75.test.ts` vitest 用例入库（待 pnpm i 后执行）
- [x] 2026-09-11 运行验证：独立 node ESM 脚本 **27/27 全绿** + CLI 冒烟 10/10 + 全仓 12 包 + CLI + mobile 构建全绿 + benchmark smoke 30/30 + 双示例回归通过

### M4.75 轮决策记录
- **Node createECDH 不支持 X25519**：改 P-256（prime256v1）；P-256 私钥即 32B 标量（JWK.d）可直接 setPrivateKey，但公钥必须 X+Y 双坐标组装 65B 未压缩点（`0x04||X||Y`），JWK.x 单独不足以 computeSecret —— `IdentityKeys` 因此增加 `encPubY`。
- **幂等 result 需 await 回传**：RequestDeduper 缓存的是 Promise，直接塞进 `ok` 字段会把 Promise 外泄给对端，`handleInvoke` 改为 `ok: await dedup.result`。
- **验证脚本 async 必须 await**：ok 包装器不 await 时各测试交错执行、帧计数竞态（同一断言时而 6 时而 8），全改 `await ok(...)` 后稳定全绿。
- **tsconfig 必须排除 __tests__**：protocol 缺 `exclude: ["src/**/__tests__/**"]`（core 有），测试文件被打进 dist 触发 TS5055；补上后全仓构建恢复。

---

## 五、关键决策记录（M1 轮）

- **零重依赖策略**：OpenAI 兼容 Provider 用 Node 内置 `fetch` + 手写 SSE 解析，不引 `openai` SDK；glob 自实现；工具参数校验用轻量手写校验而非 ajv。契合 ADR-008。
- **ScriptedProvider 优先**：测试基建先行，所有 loop 行为用确定性回放，零 API 成本。
- **M1 不含**：PatchEngine（M2）、沙箱 L2/L3（M3）、MCP（M3.5）、压缩（M2）、子智能体（M2）。引擎 API 预留扩展点。

### M2 轮补充决策

- **子智能体 = 同实例 `engine.run()` 另一次调用**（Supervisor 模式），不引入第二套引擎；父子 session 锁按 sessionId 独立，天然支持嵌套。
- **桥接事件走宿主实时通道**：父 `run()` 生成器在 `await` 工具执行期间无法 yield，故 `subagent.*` 事件经 `onEvent`/`emitLive` 即时送达；`task` 调用并入「读并行组」（meta 级），受 Supervisor 并发槽控流。
- **审批冒泡消竞态**：冒泡事件同步送达后 UI 可能立即 `resolve`，早于子会话 `approval.request` 注册；`InteractiveApprovalGateway` 增加早期裁决缓存。
- **每轮派发计数同步预留**：并行 `task` 调用下 `subSpawnCount` 读取存在竞态，改为 Supervisor 内同步占位的 `turnSlots`，turn 边界重置。
- **子会话事件单一事实源**：引擎按 `session.id` 落盘 `subs/<id>/events.jsonl`，Supervisor 不再重复 append（否则事件翻倍）。
- **首次压缩不受 minInterval 约束**：`turnsSinceLastCompact = -1` 哨兵表示「从未压缩」。

---

## 六、下一步（全部里程碑完成 → 运营收尾）

- M4.75 全部交付：远程协议 + E2E 加密 + 中继 + CLI 设备命令族 + 移动端骨架（27/27 全绿）；全仓 12 包 + CLI + mobile 构建全绿，benchmark smoke 30/30，双示例回归通过
- 剩余项：M4.9 学习扩展（用户决策跳过）；M5 运营项（1.0.0 发布 / 30 天 live 跑分 / 趋势图）待用户启动
- 待用户环境提供依赖后可做收尾：`pnpm i` 安装 electron/react/vite/ws → 真实 WSS + Expo 接入 + Electron 窗口 + 自动更新 + vitest 全量测试
- 真实 LLM 端到端验证仍需用户提供 API Key（含 benchmark live 跑分需配置 MOZI_BENCH_* secrets）

> AI生成