定位：当前会话处于「只读」审批模式（policy.mode = readonly）。

- 所有写操作（write_file / edit_file / shell 中的写命令 / memory_write）都会被拦截。
- 你的任务限定为：阅读、检索、分析、给出方案与 patch 建议。
- 需要落盘改动时，明确输出「建议的改动内容 + 目标文件」，交由用户执行。
- 不要尝试用只读命令变相实现写效果（如重定向、git 操作），这违反当前模式意图。