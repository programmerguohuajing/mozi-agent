---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'af6eaa92-d196-4e15-a0e4-af09868523c7'
  PropagateID: 'af6eaa92-d196-4e15-a0e4-af09868523c7'
  ReservedCode1: 'cc564878-6679-462b-ab9c-20c0daa514ed'
  ReservedCode2: 'cc564878-6679-462b-ab9c-20c0daa514ed'
---

# 2026-09-11 工作日志

## M3 沙箱与生态 + M3.5 完整 MCP 客户端（完成）

### M3 沙箱（@mozi/sandbox）
- `shared/src/sandbox.ts` 新增跨层契约：SandboxLevel / SandboxOptions / SandboxResult / SandboxRunner（含 AbortSignal）。
- `sandbox/src/`：platform.ts（平台探测 + docker）/ network.ts（白名单）/ exec.ts（L0-L3 + AbortSignal + 进程组强杀）/ select.ts（选择链 + 优雅降级）/ index.ts。
- 接线：tools/types.ts 的 ToolContext.sandbox、shell.ts 经沙箱执行并回填 meta.sandbox*、shared/messages.ts 的 ToolResult.meta 扩展。
- core：EngineDeps.sandbox 透传 + factory 的 sandboxLevel / allowNet 选项。

### M3 会话 fork/GC（core/src/session/session-store.ts）
- 新增 fork(srcId, atEventIndex?, newId?) 复制前 N 条事件 + meta（记 forkedFrom）。
- 新增 gc(retentionDays=30) 移入 .trash、restore(id)、listTrashed()；list() 过滤点目录。

### M3.5 完整 MCP 客户端（@mozi/mcp-client）
- transport/：types.ts 抽象 + stdio.ts（换行分隔 JSON、stderr 转日志、SIGTERM→1s→SIGKILL、Windows .cmd）+ streamable-http.ts（POST + Mcp-Session-Id + SSE 解析 + GET 推送流 + DELETE、旧版 HTTP+SSE 降级）+ mock.ts。
- jsonrpc.ts（请求/响应关联、超时、AbortSignal、反向请求路由）、connection.ts（initialize 握手 + 能力协商、30s ping、状态机 disconnected→…→offline、sampling/elicitation/roots 反向请求、tools/resources/progress/logging/cancelled 通知）。
- tools.ts（McpToolAdapter 前缀隔离 + content 渲染 + image/audio 落盘）、bridge.ts（McpBridge 多 server 聚合 + tools 热更新 + prompts→/mcp: 命令 + 内置 mcp_read_resource + $VAR 解析）、oauth.ts（PKCE + RFC9728 发现 + token 刷新 + TokenStore）。
- shared/events.ts 新增 mcp.server.status / mcp.sampling.requested|resolved / mcp.elicit.requested|resolved。
- core/factory.ts：mcpServers / onMcpEvent 选项 + createEngineAsync() + 暴露 McpBridge + dispose()。

### 关键工程决策 / 踩坑
- **构建入口（重要）**：仓库没有根 solution tsconfig，`tsconfig.base.json` 仅供 extends。
  ❌ 绝不能跑 `tsc -b tsconfig.base.json` —— 它被当作普通工程且无 outDir，会把 `.js/.d.ts` 吐到**每个源文件旁**（曾污染 288 个文件）。
  ✅ 正确做法：逐个包 `tsc -b packages/<pkg>/tsconfig.json`。按此方式重建后 stray = 0。
- 仓库未跑 pnpm install，node_modules 无 @mozi/* symlink → tsc NodeNext 解析 workspace 包失败。
  解法：各 package tsconfig 加 baseUrl + paths 指向 `../<pkg>/dist/index.d.ts`；运行时用 node 建 junction symlink（D:/codex/mozi/node_modules/@mozi/*）。
- git 仓库只 commit 了各包的 `index.ts` 骨架，其余 `src/**` 均为未跟踪 WIP（这是仓库既有状态，非本次引入）。
- tsconfig.base 开启 noUncheckedIndexedAccess：args[0] / tools[0] 需 `!` 或 `?.`。
- 类型归属：JSONSchema 在 @mozi/tools；ToolResult 在 @mozi/shared；ChatMessage 在 @mozi/shared（core/compactor.ts 原错从 @mozi/providers 导入，已修正）。
- 环境：bash 的 coreutils（dirname/head/cat）损坏，但绝对路径 node.exe 可用。构建用 `node tsc.js -b` 并把输出重定向到文件再 Read。
- 无 pnpm 环境下，用独立 node ESM 脚本 + 回环 transport 替代 vitest 跑运行时验证。

### 验证结果
- 逐包 `tsc -b packages/<pkg>/tsconfig.json` 全部 EXIT 0（12 个包）。
- 最终综合验证 13 断言全通过（沙箱导出/L0 执行、MCP 握手/状态事件/适配器执行、会话 fork/gc/restore）。
- docs/开发任务.md 已更新 M3 / M3.5 章节与里程碑状态。

## M2 未完成任务补全（完成）

开工前先盘点：RiskAnalyzer（policy/src/risk-analyzer.ts）、PolicyRule 自定义（§6.1）、compactor（core/src/context/compactor.ts）、FreshnessTracker（core/src/context/freshness.ts）均已存在。真正缺的只有四件事，已全部落地：

### M2-A Auto-Compact 接入引擎（core/src/engine/agent-engine.ts）
- run() 每步前调用 `maybeCompact(session, tracker, sid, emit)`：`shouldCompact`（usage/budget ≥ 0.8 且间隔达标）→ `compactMessages` → splice 替换最早 40%（偶数对齐、保留 ≥5 条）为 `<context-summary>` → emit `context.compacted { removedTurns, savedTokens, summary }`；失败仅跳过不中断。
- **哨兵值坑**：`turnsSinceLastCompact` 初值必须为 `-1`（表示"从未压缩"），且 `shouldCompact` 需 `>= 0` 才做最小间隔拦截，否则首轮压缩永远被 min-interval 挡住。一轮结束 `saveMeta` 持久化 compactCount。

### M2-B todo_list 工具（packages/tools/src/todo-list.ts）
- `operation: 'list'|'update'`，`TodoTask { id, title, status: pending|in_progress|done, detail? }`，渲染 `[ ]/[~]/[x]`。
- 状态存 session.meta.todos，`resume` 时从 meta.json 恢复；executeOne 中 `todo_list` 执行后立即 saveMeta。

### M2-C 引擎级文件新鲜度注入
- `ToolResult` 路径：read_file/edit_file → `tracker.markRead(absPath)`；shell → `tracker.checkDirty()`。
- `ContextManager.build(session, { dirtyFiles })` 把脏文件清单作为 system 通知注入视图。

### M2-D 子智能体编排 v1（core/src/subagent/）
- templates.ts：`AgentTemplate` + 内置 explore（readonly 32k/30）/ general（64k/50）/ reviewer（readonly 32k/20）；`loadAgentTemplates` 扫 `.mozi/agents`、`.claude/agents`、`~/.mozi/agents`（frontmatter 解析）。
- supervisor.ts：`SubAgentSupervisor implements SubAgentDispatcher`。子智能体 = 又一次 `AgentEngine.run()`（Supervisor 模式）；`child = min(parent, template)` 策略收紧 + 工具交集；FIFO Semaphore（带队列位次）；depth ≤ 3、每轮并发 ≤ 2、全局 ≤ 8、超时 300s；子会话持久化到 `subs/<id>/events.jsonl`。
- tools/task.ts：`task` 工具（riskLevel `meta`，registry 中与 read 同为可并行组）。
- shared：RunInput.session（注入内存中的子会话）、ToolResult.meta.subSessionId、6 个 `subagent.*` 事件。

### 关键架构决策：审批冒泡走"宿主实时通道"
- 问题：父 `run()` 生成器正阻塞在 `executeOne → tool.execute → supervisor`，子智能体的审批事件**没法经父的 yield 流送出**。
- 解法：新增 `AgentEngine.setHostEventSink(onEvent)` / `emitLive(event, originSessionId?)`，子智能体事件绕开生成器直接打到宿主 sink；factory 增加 `onEvent` 选项，CLI 用它做冒泡渲染。supervisor 只 emit 不再 appendEvent（事件唯一来源是 engine.log，避免重复）。
- 配套竞态修复：`InteractiveApprovalGateway` 增加 `early` map —— sink 可能在 `approval.request` 注册前同步触发；`maxPerTurn` 用同步 `turnSlots` 预留（逐轮 reset）修并行 `task` 竞态。

### 验证结果（M2）
- 运行时 3 套 69/69 断言通过（M2-A/B/C/D 50 + 冒泡/并发/取消 16 + 新鲜度 3）。
- 全仓 vitest 112 测试、7 个文件全通过；12 包 + CLI 构建 EXIT 0。临时 `_tmp_*.mjs`/日志已清理。
- docs/开发任务.md：M1/M2 行标 ✅；§四 拆成 T2/T3/T4 全部完成；§五 补"M2 轮补充决策"；§六 改为"M2 已完成 → M4 桌面 / M4.5 定时任务"。

## M4 桌面应用（完成）

设计依据 docs/Mozi-详细设计文档.md §M10。无 pnpm install（electron/react/vite 未装），故将**传输无关核心**全部落地为纯 Node 可测代码，Electron 外壳用晚绑定最小接口 + ambient 类型垫片保 tsc -b 全绿。

### 交付面
- @mozi/protocol：19 条 invoke + 4 条 send 泛型契约（InvokeChannels/SendChannelMap）、INVOKE_CHANNELS/SEND_CHANNELS 清单、ChannelServer/ChannelClient、MaybePromise、approvalTicketFromEvent、LoopbackChannel（loopback.ts）。移动端协议=桌面 IPC=loopback，零分叉（§14 原则3）。
- desktop/src/main/agent-service.ts：会话池（每会话独立 CreatedEngine）→ 多会话并行；start() 立即返回 + 后台 drive() 驱动生成器；stamp() 给引擎事件补 sessionId（多会话 UI 分桶）；attach/detachWindow、shutdown、dashboard、audit。
- ipc-bridge.ts（19 通道落 AgentService + 事件扇出）、electron-transport.ts（ipcMain/webContents 适配 + 'mozi:' 前缀）、preload.ts（白名单 ALLOWED_INVOKE/SEND）、electron-main.ts（晚绑定 electron boot）。
- diff-service.ts：LCS 最小编辑脚本 → buildHunks（连续变更聚合/稳定 h1..hN id/行范围）+ applyHunksToContent（仅选中 hunk）+ DiffReviewService(record/get/applyPartial/applyAll/clear)。
- shared/diff-view.ts：languageFor + hunkDecorations（主/渲染共用，渲染侧不再自带一份）。
- settings-store.ts：SettingsStore（原子落盘）+ SafeStorageLike 抽象 + safeStorage 不可用拒绝明文 + 环境变量兜底 + maskSecret + listProviders（只回脱敏）。
- mcp-manager.ts：list/add/remove/restart + 三档 sampling + trusted + allowedTools。
- shared/render-item.ts（RenderItem 联合 + eventToRenderItems）、shared/store.ts（UiStore 纯 reducer + useSyncExternalStore 订阅）。
- renderer/：App/Sidebar/SessionView/SettingsPanel/Dashboard/SubAgentPanel/ApprovalCard/DiffReviewer + react-shim.d.ts（无 react 依赖下 tsc --noEmit 通过）+ index.html(CSP) + vite.config.ts + vitest.config.ts。
- __tests__/m4.test.ts：47 用例全通过。全仓 159 测试通过，9 包 + 渲染进程构建 EXIT 0。

### 3 个真 bug（修在 core/desktop，非测试）
1. **审批死锁**：AgentService 后台消费生成器，而 executeOne 等审批时事件只在生成器 yield、未上抛 → 宿主与引擎互等。修：engine 的 push() 对 tool.approval.required|resolved 额外走 hostSink（agent-engine.ts）。
2. **密钥回退缺陷**：safeStorage 不可用时 getApiKey 把密文当明文返回，会让上游把 ciphertext 当 API Key 发出。修：不可解密即视为无密钥，回退环境变量。
3. **事件双份**：审批经宿主通道上抛后 resolveApproval 又手动 emit 一次。修：移除手动 emit + pool/store 按 callId 去重。

### 测试踩坑
- ScriptedProvider 的 ScriptedToolCall.riskLevel 缺省 'read'（scripted.ts build），auto 模式下 read=allow 不触发审批 → 测试须显式 riskLevel:'exec'。
- 测试里 shell 若真执行 git push 会拖慢/挂起；approval 用例 resolve 后直接 shutdown(500) 收尾，afterEach 加 EBUSY 短重试。
- desktop 需独立 vitest.config.ts（否则自动加载 vite.config.ts → 找不到 vite）。

## M5 开源运营与评测体系（代码侧收尾 + 提交）
- M5 代码侧此前已在工作区落地（benchmark/examples/website/.github/治理文档），但**从未 commit**（git log 仅 M0 5 条）。
- 本轮完成交付物实测：benchmark smoke 30/30 全绿（L1 10/10、L2 10/10、L3 5/5、L4 5/5）；examples/embed-minimal、examples/custom-tool 均跑通；website 文档站 vitepress build 成功（12 个 HTML）。
- 提交范围：全部 M1–M5 实现 + M5 交付物；**排除** outputs/（M4 草稿副本）与 .workbuddy/（智能体私有记忆，不入公开仓库）。
- M5 剩余运营项（1.0.0 发布 / 30 天跑分 / 趋势图 / 定时运营化）为时间与用户决策项，不在代码侧。

## M4.5 定时任务与无人值守（完成）
- 交付：packages/core/src/tasks/（cron.ts 零依赖 Intl 时区求解 + solver.ts 补跑策略 + store.ts 原子写 + locks.ts 三层锁 + unattended.ts 策略合成 + notify.ts webhook + headless.ts + runner.ts worktree 产物 + scheduler.ts tick 主循环）+ CLI `mozi task add/list/run/tick/doctor/gc` 命令族。
- 引擎小改：EngineDeps.evaluateOptions（`createEngine({ unattended })` → I1 ask→deny 注入 evaluate）。
- 关键坑：
  1. **cron tzToUtc 修正方向反了**：UTC 时间戳按「显示偏移」反向修正（`ts += (h-p.h)*3600000 + (mi-p.mi)*60000`），符号反了会震荡超限。
  2. **commander 父子 option 同名冲突**：顶层 `--policy` 与 `task add --policy` 同名时父命令抢占参数（子命令拿到默认值）→ 顶层 exec 改 `--policy-mode`。
  3. **once 过期任务首次 tick 不触发**：planTick 首次调度分支需特判 once-due（立即补跑 + 跑完 disable）。
  4. **headless 成功判定**：引擎正常结束 reason 是 `model_finished`（不是 success）。
  5. **tsc -b 增量缓存**：手动改 dist 后必须删 tsbuildinfo/dist 强制重编；本机 tsc 增量对中文输出文件不敏感。
  6. **PowerShell Get-Content -Raw 默认 ANSI 读**：读写 UTF-8 文件会破坏中文（曾破坏 dist/task.js），读源码用 Read 工具，写文件用 WriteAllText(UTF8)。
- 预存问题修复：screenshot.ts 的 ok(meta.kind:'image') 应为 display（重建 shared 后解决）；core/index.ts 移除未完成 memory 导出（与 compactor estimateTokens 冲突）。
- 验证：独立 node ESM 脚本 21/21 全绿（vitest 因 pnpm 依赖缺失不可用）；CLI 冒烟 task add/list/doctor 正常；全仓 12 包 + CLI 构建 EXIT 0。

> AI生成
## M4.75 移动端与远程访问（完成）
- 交付：@mozi/protocol（remote.ts 设备权限/配对/注册表/事件游标/幂等/附件外置 + crypto-box.ts E2E 加密 + remote-node.ts 节点服务）+ @mozi/relay-server（RelayRouter 零内容路由 + NtfyPushGateway）+ CLI（mozi device pair/list/revoke/rename + mozi serve）+ apps/mobile 骨架（client.ts 视图模型 + RemoteClient 抽象）。
- 关键坑：
  1. **Node createECDH 不支持 X25519**（ERR_CRYPTO_OPERATION_FAILED）→ 换 prime256v1（P-256）；P-256 私钥=32B 标量可直接 setPrivateKey，但公钥需 X+Y 双坐标组装 65B 未压缩点（0x04||X||Y），JWK.x 单独不足 → IdentityKeys 增 encPubY。
  2. **幂等 ok 外泄 Promise**：RequestDeduper 缓存的是 Promise，handleInvoke 直接塞 ok 字段导致对端收到 Promise → 改 `ok: await dedup.result`。
  3. **验证脚本 async 竞态**：ok() 包装器不 await 时测试交错执行、帧计数错乱（6!==8 假失败）→ 全部改 `await ok(...)`。
  4. **tsconfig 排除 __tests__**：protocol 缺 exclude 配置，测试文件进 dist 触发 TS5055，core 有 `"exclude": ["src/**/__tests__/**"]`，protocol 补上。
- 验证：独立 node ESM 27/27 全绿（权限矩阵/配对 TTL·单次·暴力锁定/注册表/E2E P-256/中继/协议全链路含幂等计数）；CLI 冒烟 10/10；全仓 12 包 + CLI + mobile 构建 EXIT 0；benchmark smoke 30/30；examples 双实例回归通过。
- vitest 用例 packages/protocol/src/__tests__/m4-75.test.ts 入库（待 pnpm i 后执行）。
- 文档：docs/开发任务.md 新增四之七 M4.75 章节 + 里程碑表 M4.75 ✅ + M4.9 标注用户决策跳过 + 六、下一步改为运营收尾。
- M4.9（学习扩展）按用户决策跳过，WIP 留工作区未提交。

> AI生成
## M4.9 学习扩展 M18 Hooks（完成）
- 交付：packages/core/src/hooks/（types.ts 12 钩子点 + config.ts 配置加载/防投毒/指纹确认 + runner.ts HookRunner 执行器 + index.ts 导出）+ __tests__/m4-9-hooks.test.ts vitest 用例。
- M15 提示词 / M16 记忆 / M17 多模态已在前序会话完成并提交（4e90588/27a6805/6704295），本轮完成 M18 Hooks 并将 M4.9 全部收口。
- 关键设计：Hooks 执行不过沙箱（等同用户直接执行）；退出码 0 continue / 2 block / 其他 onExit 映射；超时按 onExit[*] + 告警；连续失败 10 次自动禁用；blocking 事件 block 短路；stdout {"note":"..."} 注入下轮；防投毒三防线（项目级默认禁用→指纹确认→headless 忽略）。
- 关键坑：验证脚本防投毒测试组中前一个用例写入的 .mozi/hooks.json 污染后续用例 workspace → 用户级测试需用全新 tmp 目录。
- 验证：独立 ESM 27/27 全绿（执行语义矩阵 7 + 超时崩溃禁用 4 + note 注入 4 + glob 匹配 5 + 防投毒 6 + 审计 1）；全仓 12 包 + CLI + mobile 构建全绿；benchmark smoke 30/30；双示例回归通过。
- 文档：docs/开发任务.md 新增四之八 M4.9 章节 + 里程碑表 M4.9 ✅ + 六、下一步更新。
- 至此 M0-M5 + M4.5 + M4.75 + M4.9 全部里程碑代码侧完成。

> AI生成
## M4.9 引擎接线收口（本轮补齐）
- 补齐 M18 与引擎的四处接线（前轮只完成 hooks/ 包本身）：AgentEngine 11 触发点（session/turn/tool/approval/compact 各 pre/post，tool:pre/approval:pre block 短路，note 累积进 hookNotes 注入下轮 ContextManager.build）；ToolContext 注入 memory/vision；ContextManager.build 增 hookNotes 段；SubAgentSupervisor task:run:pre/post（含错误双路径，durationMs 前置）；createEngine 工厂组装 PromptAssembler+MemoryManager+MemoryStore+ScreenshotService+HookRunner(loadHooks, headless 忽略项目级)，辅助 memoryAccessFrom/detectGitBranch。
- 新增 __tests__/m4-9-engine-hooks.test.ts：生命周期触发/tool:pre block 写文件/memory_write 真实落库/提示词分层 L0-L3，4 用例。
- 测试断言坑：L0 identity.md 为中文「你是墨子…」，原测试 `toContain('Mozi')` 失败 → 改为 `toContain('墨子')`。
- 验证：core tsc -b EXIT 0；core vitest 全量 143 passed / 2 failed（2 个失败为预存 m4-45.test.ts WIP，超本任务范围，不修）；M4.9 专项 27+4=31 全绿。
- 文档：docs/开发任务.md 补 T5 引擎接线小节 + 里程碑表/六、下一步 改 31 断言全绿。

> AI生成